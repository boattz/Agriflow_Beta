// Agriflow secrets (DO NOT COMMIT — copy from secrets.h.example)
// 1. Copy this file to secrets.h (same folder)
// 2. Paste your SENSOR_API_KEY (must match Render env SENSOR_API_KEY)
// 3. Re-flash the ESP32. Leave empty ("") = send without key.
#define SENSOR_API_KEY ""
